[phases.setup]
# Pin Python to a stable version supported by Nixpacks.
# This avoids Python 3.13 runtime surprises.
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT"
